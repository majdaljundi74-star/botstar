# دليل إعداد وتشغيل البوت - Bot Setup Guide

## 📋 المتطلبات - Requirements

1. **خادم ويب يدعم PHP** (Web Server with PHP support)
   - PHP 7.0 أو أحدث
   - تفعيل cURL extension
   - تفعيل JSON extension

2. **حساب تليجرام** (Telegram Account)

---

## 🔧 خطوات الإعداد - Setup Steps

### 1. الحصول على توكن البوت - Get Bot Token

1. افتح تليجرام وابحث عن **@BotFather**
2. أرسل الأمر `/newbot`
3. اختر اسم للبوت
4. اختر معرف (username) للبوت (يجب أن ينتهي بـ `bot`)
5. سيرسلك BotFather توكن البوت (مثل: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)
6. **انسخ التوكن واحتفظ به**

### 2. الحصول على معرف المطور - Get Developer ID

1. افتح تليجرام وابحث عن **@userinfobot** أو **@getidsbot**
2. أرسل أي رسالة للبوت
3. سيرد عليك برقم معرفك (مثل: `123456789`)
4. **انسخ المعرف واحتفظ به**

### 3. تعديل ملف الإعدادات - Edit Config File

افتح ملف `config.php` وقم بتعديل القيم التالية:

```php
$TOKEN = "ضع_توكن_البوت_هنا"; // Put your bot token here
$sudoID = "ضع_معرف_المطور_هنا"; // Put your developer ID here
$botUsername = "معرف_البوت"; // Bot username without @
```

**مثال - Example:**
```php
$TOKEN = "123456789:ABCdefGHIjklMNOpqrsTUVwxyz";
$sudoID = "123456789";
$botUsername = "mybot";
```

---

## 🚀 طريقة التشغيل - How to Run

هذا البوت يعمل عبر **Webhook**، مما يعني أن تليجرام سيرسل التحديثات إلى خادمك مباشرة.

### الطريقة 1: استخدام Webhook (موصى بها) - Recommended

1. **رفع الملفات على الخادم**
   - ارفع جميع ملفات البوت على خادم ويب (مثل: cPanel, VPS, etc.)
   - تأكد أن PHP يعمل على الخادم

2. **تعيين Webhook**
   - افتح المتصفح واذهب إلى:
   ```
   https://api.telegram.org/bot<TOKEN>/setWebhook?url=https://yourdomain.com/index.php
   ```
   - استبدل `<TOKEN>` بتوكن البوت
   - استبدل `https://yourdomain.com/index.php` برابط ملف `index.php` على خادمك
   
   **مثال:**
   ```
   https://api.telegram.org/bot123456789:ABCdefGHIjklMNOpqrsTUVwxyz/setWebhook?url=https://example.com/bot/index.php
   ```

3. **التحقق من Webhook**
   - افتح الرابط التالي للتحقق:
   ```
   https://api.telegram.org/bot<TOKEN>/getWebhookInfo
   ```

### الطريقة 2: استخدام Long Polling (للتطوير المحلي) - For Local Development

إذا كنت تريد تشغيل البوت محلياً:

1. قم بتشغيل خادم محلي (مثل XAMPP, WAMP, أو PHP built-in server)
2. استخدم أداة مثل **ngrok** لإنشاء نفق:
   ```bash
   ngrok http 80
   ```
3. استخدم رابط ngrok في تعيين Webhook

---

## ✅ التحقق من عمل البوت - Verify Bot is Working

1. افتح تليجرام وابحث عن بوتك
2. أرسل `/start` للبوت
3. إذا استجاب البوت، فذلك يعني أنه يعمل بشكل صحيح

---

## 📁 هيكل الملفات - File Structure

```
Botar-master/
├── config.php          # ملف الإعدادات الرئيسي
├── index.php           # نقطة دخول البوت
├── util.php            # وظائف مساعدة
├── Lang/               # ملفات اللغات
├── Logs/               # سجلات البوت
├── Plugins/            # إضافات البوت
└── README.md           # وثائق البوت
```

---

## 🔒 الأمان - Security

- **لا تشارك توكن البوت أبداً** مع أي شخص
- تأكد من حماية ملفات البوت بصلاحيات مناسبة
- استخدم HTTPS لـ Webhook URL

---

## 🆘 حل المشاكل - Troubleshooting

### البوت لا يستجيب
- تأكد من صحة التوكن
- تأكد من أن Webhook تم تعيينه بشكل صحيح
- تحقق من سجلات الأخطاء في مجلد `Logs/`

### خطأ في الاتصال
- تأكد من أن الخادم يدعم HTTPS
- تأكد من أن PHP cURL extension مفعل
- تحقق من إعدادات Firewall

---

## 📞 الدعم - Support

إذا واجهت أي مشاكل، تحقق من:
- ملف `README.md` للوثائق الكاملة
- سجلات الأخطاء في مجلد `Logs/`

---

**ملاحظة:** تأكد من ملء جميع الحقول المطلوبة في `config.php` قبل تشغيل البوت!

